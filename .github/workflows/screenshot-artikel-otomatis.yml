name: 📸 Ambil Screenshot File HTML Artikel
on:
  workflow_dispatch:
    inputs:
      html_glob:
        description: 'Pola file HTML untuk di-screenshot'
        required: true
        default: 'artikel/**/*.html'

jobs:
  screenshot_proses:
    runs-on: ubuntu-latest
    steps:
      - name: ⬇️ Checkout Kode Repositori
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: ⚙️ Siapkan Node.js & Instal HTTP Server
        uses: actions/setup-node@v4
        with:
          node-version: '20'
      - run: npm install http-server

      - name: 💾 Instal Playwright (Modul Lokal)
        run: npm install playwright

      - name: ▶️ Jalankan HTTP Server di Background
        run: |
          nohup ./node_modules/.bin/http-server . -p 8080 --silent &
          echo "Server dijalankan di port 8080."
          
      - name: 🔍 Temukan, Periksa, dan Proses File HTML
        # ID sudah diletakkan di sini, tempat yang benar:
        id: process_files 
        run: |
          CHANGES_MADE=false
          mkdir -p artikel
          
          PLAYWRIGHT_IMAGE="mcr.microsoft.com/playwright:v1.55.1-jammy" 
          
          if [ ! -f "ext/screenshot.js" ]; then
            echo "::error::File 'ext/screenshot.js' tidak ditemukan. Harap tambahkan skrip secara manual."
            exit 1
          fi
          
          find artikel/ -name "*.html" | while read -r HTML_FILE; do
              
              BASE_NAME=$(echo "$HTML_FILE" | sed -e 's|^artikel/||' -e 's|.html$||')
              FILE_NAME=$(echo "$BASE_NAME" | sed 's|[^a-zA-Z0-9_-]|_|g')
              FINAL_PATH="artikel/${FILE_NAME}.jpg"
              TEMP_OUTPUT_INSIDE_DOCKER="temp_screenshot.jpg" 
              
              echo "--- Memproses $HTML_FILE -> $FINAL_PATH ---"
              
              if [ -f "$FINAL_PATH" ]; then
                  echo "Screenshot JPG sudah ada: $FINAL_PATH. Dilewati."
              else
                  LOCAL_URL="http://localhost:8080/$(echo "$HTML_FILE" | sed 's|^./||')"
                  INSTALL_DIR="/tmp/playwright-install"
                  
                  echo "Mengambil screenshot..."
                  
                  docker run --rm \
                    -v "${PWD}:/workspace" \
                    --network host "$PLAYWRIGHT_IMAGE" \
                    bash -c "\
                      npm install playwright --no-save --prefix $INSTALL_DIR > /dev/null && \
                      export NODE_PATH=$INSTALL_DIR/node_modules && \
                      node /workspace/ext/screenshot.js '$LOCAL_URL' '/workspace/$TEMP_OUTPUT_INSIDE_DOCKER'\
                    "
                  
                  DOCKER_EXIT_CODE=$?
                  
                  if [ $DOCKER_EXIT_CODE -eq 0 ] && [ -f "$TEMP_OUTPUT_INSIDE_DOCKER" ]; then
                      mv "$TEMP_OUTPUT_INSIDE_DOCKER" "$FINAL_PATH"
                      echo "[SUCCESS] Screenshot baru berhasil disimpan: $FINAL_PATH"
                      CHANGES_MADE=true
                  else
                      echo "::warning::[FAILED] Gagal memproses $HTML_FILE. Docker Exit Code: $DOCKER_EXIT_CODE. File output $TEMP_OUTPUT_INSIDE_DOCKER hilang."
                      rm -f "$TEMP_OUTPUT_INSIDE_DOCKER"
                  fi
              fi
          done
          
          if [ "$CHANGES_MADE" = true ]; then
             echo "changes_made=true" >> $GITHUB_OUTPUT
          else
             echo "changes_made=false" >> $GITHUB_OUTPUT
          fi

      - name: ⬆️ Commit dan Push Screenshot Baru
        # Gunakan 'always()' atau 'success()' saja, dan biarkan Git yang memutuskan.
        # Kita akan menghapus kondisi if: agar pesan commit muncul
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action Bot"
          
          # Menambahkan semua file .jpg yang mungkin baru
          git add artikel/*.jpg
          
          # Commit dengan || true agar tidak gagal jika tidak ada perubahan
          git commit -m "feat: Otomatis menambahkan screenshot baru dari workflow" || true
          
          # Push hanya jika ada sesuatu yang di-commit
          if git log -1 --pretty=%B | grep -q "feat: Otomatis menambahkan screenshot baru dari workflow"; then
              git push
          else
              echo "Tidak ada file screenshot baru yang di-commit. Proses selesai."
          fi
