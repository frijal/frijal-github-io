/* Fixed quick nav per request */
.nav-float{
  position:fixed;z-index:1100;
}
.nav-float.sitemap{left:10px;top:30%}
.nav-float.home{right:10px;bottom:20%}
.nav-float a{
  display:inline-flex;align-items:center;gap:.6rem;
  background:var(--accent);color:#fff;text-decoration:none;
  padding:.6rem 1rem;border-radius:10px;font-weight:800;box-shadow:var(--shadow)
}
.nav-float a svg{width:18px;height:18px;fill:#fff}
